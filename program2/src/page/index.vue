<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>KcalFit</title>
    <link rel="stylesheet" href="static/css/main.css">
    <style>
      #container{
        margin-top: 100px;
      }
      #menu-btn{
        display: flex;
        justify-content: space-around;
      }
      #menu-btn li{
        display: inline-block;
        padding: 10px;
        text-align: center;
        width: 150px;
      }
      a{
          display: flex;
          justify-content: center;
          align-items: center;
          border: 6px solid white;
          width: 170px;
          height: 50px;
          border-radius: 34px;
          box-shadow: 0 1px 0 rgba(255, 255, 255, 0.34) inset, 0 2px 0 -1px rgba(0, 0, 0, 0.13), 0 3px 0 -1px rgba(0, 0, 0, 0.08), 0 3px 13px -1px rgba(0, 0, 0, 0.21);
      }
      a:hover{
          background-color: #fff4b7 !important;
      }
    </style>
</head>
<body>
    <div id="container">
      <h1>칼로리핏</h1>
      <ul id="menu-btn">
        <a href="/diet" style="background-color: pink;"><li>식단</li></a>
        <a href="/moisture"  style="background-color: rgb(192, 238, 255);"><li>수분</li></a>
        <a href="/exercise" style="background-color: rgb(205, 255, 192);"><li>운동</li></a>
        <a href="/alarm" style="background-color: rgb(242, 192, 255);"><li>알림</li></a>
    </ul>
    </div>
    <!-- <h1>환영합니다!</h1>
    <button onclick="fetchData()">데이터 불러오기</button>
    <div id="data"></div>
  
    <script>
      function fetchData() {
        fetch('/data')
          .then(response => response.json())
          .then(data => {
            const userDataDiv = document.getElementById('data');
            
            if (data.data.length > 0) {
                // HTML에 표시할 내용 생성
                let userInfo = '<ul>';
                data.data.forEach(user => {
                    userInfo += `<li>ID: ${user.id}, UserID: ${user.userid}, Password: ${user.userpw}</li>`;
                });
                userInfo += '</ul>';
                
                // 데이터를 페이지에 표시
                userDataDiv.innerHTML = userInfo;
            } else {
                userDataDiv.innerHTML = 'No user data available';
            }
          });
      }
    </script>
   -->
</body>
</html>
